# tests/regression/test_scapy_tools.yaml
task:
  prompt: >
    Here is your task:
    1. Use the 'scapy_ping' tool to ping the target '127.0.0.1'.
    2. For verification, check that the output string contains 'is up'.
    3. If the verification is successful, use the 'finish' tool with a status of 'success'.

config: "verified_flow"

execution:
  iterations: 5
  safe_mode: false
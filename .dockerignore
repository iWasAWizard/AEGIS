# .dockerignore
# Exclude files from the Docker build context to speed up builds and reduce image size.

# Docker
.dockerignore
Dockerfile*
docker-compose*

# IDEs and editors
.idea/
.vscode/
*.swp
*.swo
*~

# Exclude Git data
.git
.gitignore
.gitattributes

# Exclude local Python virtual environments
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.pytest_cache/
.venv/
venv/
env/

# Exclude local development and test files
.idea/
.vscode/
*.swp
*.swo

# Exclude local output directories (these are mounted as volumes, not needed in the build)
logs/
reports/
artifacts/
index/

# Model files, until this blows up in my face.
*.gguf
